security-poll-suffix=
launch=remote
distributor-threads=3
remote-connection-string=http:url=http://host.docker.internal:8053/,post=yes,post_json=yes,something=useless
# for integration testing we need to disable any caching, but all valid cache settings are determined in the testcontainers configuration and added to a cache-settings.conf in the include-dir dynamically
include-dir=/etc/powerdns/pdns.d
